@using Shared.Models
@model List<PendingRegistrationDto>

<h2 class="mb-4">Pending Registrations</h2>

<div class="mb-3">
    <a asp-controller="Admin" asp-action="Manage" class="btn btn-primary">Manage AD</a>
</div>

@if (!Model.Any())
{
    <div class="alert alert-info">
        No pending requests
    </div>
}
else
{
    <table class="table table-hover table-striped shadow-sm">

        <thead class="table-dark">
            <tr>
                <th>First Name</th>
                <th>Last Name</th>
                <th>Username</th>
                <th>Actions</th>
            </tr>
        </thead>

        <tbody>
            @foreach (var r in Model)
            {
                <tr>
                    <td>@r.FirstName</td>
                    <td>@r.LastName</td>
                    <td>@r.Username</td>

                    <td>
                        <form method="post" asp-action="Approve" class="d-inline">
                            <input type="hidden" name="id" value="@r.Id" />
                            <button class="btn btn-success btn-sm">Approve</button>
                        </form>

                        <form method="post" asp-action="Reject" class="d-inline">
                            <input type="hidden" name="id" value="@r.Id" />
                            <button class="btn btn-danger btn-sm">Reject</button>
                        </form>
                    </td>
                </tr>
            }
        </tbody>

    </table>
}